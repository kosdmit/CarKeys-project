upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name carkeysamara.ru www.carkeysamara.ru carkeysamara.online www.carkeysamara.online 185.14.45.230;

    # Redirect HTTP to HTTPS (uncomment when SSL is ready)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /vol/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /vol/media/;
        expires 1y;
        add_header Cache-Control "public";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
}

# Uncomment for SSL configuration
# server {
#     listen 443 ssl http2;
#     server_name carkeysamara.ru www.carkeysamara.ru carkeysamara.online www.carkeysamara.online 185.14.45.230;
#
#     ssl_certificate /etc/letsencrypt/live/carkeysamara.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/carkeysamara.ru/privkey.pem;
#
#     # SSL configuration (use modern settings)
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512;
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }